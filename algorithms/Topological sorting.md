# Topological sorting

Direct acyclic graphs can be sorted such that, for every edge `uv` in the graph, `u` appears in the sorted order before `v`.  See https://www.geeksforgeeks.org/topological-sorting/ for example.